name: data_management
description: Collection of service with advanced style and controlling system.
version: 0.2.1
repository: https://github.com/P-YMR/flutter_data_management.git

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_andomie: ^0.5.15
  firebase_database: ^10.5.1
  cloud_firestore: ^4.16.1
  cloud_firestore_platform_interface: ^6.1.13
  dio: ^5.4.2+1
  localstore: ^1.3.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter: